#chapt 1 part b
print "Area of a trapezoid"
height = input("Enter the height of the trapezoid: ")
bottom_len = input("Enter the length of the bottom base: ")
top_len = input("Enter the length of the top base: ")

trap_area = 0.5*(bottom_len + top_len)*height

print 'The area is ',trap_area
